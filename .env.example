# Supabase frontend keys (公開的 anon key — 可放到 Vercel)
VITE_SUPABASE_PROJECT_ID=""
VITE_SUPABASE_URL="https://<project-ref>.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY=""

# Edge Functions 端的密鑰（在 Supabase Dashboard → Project Settings → Functions → Secrets 設定）：
# LOVABLE_API_KEY="..."
# (set-course-password 會自動使用 SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY，無需手動設)
